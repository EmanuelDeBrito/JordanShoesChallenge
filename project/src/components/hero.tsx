type Props = {
    background: string
}

export const Hero = ({ background }: Props) => {
    return(
        <div className="bg-cover bg-no-repeat bg-center h-[64px]">
            
        </div>
    )
}